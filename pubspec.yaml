name: finance_project_sophia_flutter
description: "A new Flutter project."
publish_to: 'none' # Remove this line if you wish to publish to pub.dev
version: 1.0.0+1

environment:
  sdk: '>=3.3.4 <4.0.0'


dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.6
  provider: ^6.1.2
  firebase_auth: ^5.2.0
  cloud_firestore: ^5.4.0
  firebase_core: ^3.4.0
  fl_chart: ^0.40.0
  flutter_svg: ^2.0.10+1
  intl: ^0.17.0


dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true

  assets:
    - lib/utils/images/logo.svg

